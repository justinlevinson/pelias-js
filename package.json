{
  "name": "pelias-js",
  "version": "0.0.5",
  "description": "A JS client for the Pelias geocoder",
  "main": "index.ts",
  "repository": "https://github.com/justinlevinson/pelias-js",
  "author": "Justin Levinson (justinlevinson)",
  "license": "GPL-3.0-or-later",
  "private": false,
  "scripts": {
    "build": "node_modules/typescript/bin/tsc -p ./tsconfig.build.json",
    "start": "npm run build && node build/index.js",
    "test": "node_modules/jest-cli/bin/jest.js --config=./test/jest.config.js",
    "build:watch": "node_modules/typescript/bin/tsc -p ./tsconfig.build.json -w --preserveWatchOutput"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/jest": "^24.0.9",
    "jest": "^24.0.0",
    "jest-cli": "^24.0.0",
    "ts-jest": "^24.0.0",
    "ts-node": "^8.0.2",
    "typescript": "^3.2.4"
  },
  "files": ["build/**/*"]
}
